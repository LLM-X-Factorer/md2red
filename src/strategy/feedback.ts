export async function buildFeedbackContext(): Promise<string | null> {
  // Feedback context requires published note metrics which are no longer tracked.
  // This will be replaced with a different feedback mechanism in the future.
  return null;
}
